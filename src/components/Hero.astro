<div class="hero__image">
  <div class="hero__wrapper">
    <img class="hero__logo" src="/images/vurfc-logo.png" alt="Hero logo" />
    <h1 class="hero__title">2024 FALL 15'S RUGBY SEASON</h1>
    <a href="/schedule" class="hero__link">
      <button class="hero__link-button">REGISTER NOW</button></a
    >
  </div>
</div>

<style>
  .hero__image {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  .hero__image::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url(/images/hero-image.jpg);
    background-size: cover;
    background-position: center;
    filter: blur(1px); /* Apply blur effect */
    z-index: 1;
  }

  .hero__image::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent grey tint */
    z-index: 2;
  }

  .hero__wrapper {
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    z-index: 3;
    padding-inline: 1em;
  }

  .hero__logo {
    position: relative;
    z-index: 3;
    width: 150px;
    padding-top: 1em;
  }

  .hero__title {
    color: inherit;
    font-size: var(--fs-700);
    font-weight: var(--fw-bold);
    text-align: center;
    z-index: 3;
    margin-inline: 1em;
  }

  .hero__link-button {
    position: relative;
    cursor: pointer;
    background-color: var(--clr-gold);
    color: var(--clr-black);
    padding: 10px 30px;
    border: none;
    border-radius: 40px;
    font-family: var(--ff);
    font-size: var(--fs-800);
    text-transform: uppercase;
    box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
    letter-spacing: 1px;
    z-index: 3;
  }

  .hero__link {
    position: relative;
    z-index: 3;
    padding-bottom: 1em;
  }
</style>
